<Window x:Class="QuillApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuillApp"
        mc:Ignorable="d"
        Title="Quill App" Height="450" Width="800">
    <Window.Resources>
        <local:RenderVM x:Key="renderVM"/>

    </Window.Resources>
    <Grid DataContext="{StaticResource renderVM}">
        <Image x:Name="imgelt" Source="{Binding Image, Mode=TwoWay, NotifyOnSourceUpdated=True}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseLeftButtonDown">
                    <i:InvokeCommandAction Command="{Binding MouseClick}"  />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Image>
    </Grid>
</Window>
